version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    volumes:
      - ./output:/app/output
      - ./spotify_output:/app/spotify_output
    restart: unless-stopped